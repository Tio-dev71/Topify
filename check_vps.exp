#!/usr/bin/expect -f
set timeout 60
spawn ssh root@157.66.100.35 "pm2 status Topify && pm2 logs --lines 20 --nostream Topify"
expect {
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "Topmediacto!!!\r"
        exp_continue
    }
    eof
}
