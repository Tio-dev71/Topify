#!/usr/bin/expect -f
set timeout 20
spawn ssh root@157.66.100.35 "cd /var/www/topify && pm2 logs --lines 50 --nostream Topify"
expect {
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "Topmediacto!!!\r"
        exp_continue
    }
    eof
}
